# Secure-Login-Sys
A secure Flask web app featuring Role-Based Access Control (RBAC). It uses Flask-Bcrypt for password hashing and Flask-JWT-Extended for session security. Includes an account lockout mechanism after 5 failed attempts to prevent brute-force attacks and an admin dashboard for user management.
